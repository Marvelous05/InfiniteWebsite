const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "infinitebiomedsol-com.lovable.app",
        pathname: "/**"
      }
    ]
  }
};

export default nextConfig;
